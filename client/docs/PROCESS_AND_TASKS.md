## Процесс разработки

1.  Проектирование контрактов: написание дата-классов `GameState`, `Player`, `Mob`, `MapObject`, `GameStats` до реализации систем.
2.  Изоляция логики: игровая логика находится в `core/engine`, отрисовка — в `render`, UI — в `ui`, сетевой слой — в `network`.
3.  Entity-First подход: логика пишется без привязки к Compose. Визуализация состояния происходит после обновления данных в `GameState`.

## Распределение задач

Распределение отражает зоны ответственности участников в рамках MVP. Оно описывает вклад по направлениям разработки и может не совпадать один к одному с авторством отдельных pull request.

### Губанов Георгий: базовая архитектура, core engine и backend-старт

*   Настройка базовой структуры Android-клиента и начального UI.
*   Реализация основы игрового цикла и обновления состояния игры.
*   Реализация базовых игровых сущностей: игрок, мобы, объекты карты.
*   Добавление системы здоровья, атак мобов и базовой PvE-логики.
*   Первичная интеграция серверной части, авторизации и JWT.

### Чернокульская Алина: визуальная часть, сценарии, клиентские тесты и стабилизация

*   Доработка визуальной части клиента и игровых экранов.
*   Добавление новых игровых сценариев и улучшение пользовательского восприятия партии.
*   Реализация клиентских тестов для игровой логики и вспомогательных компонентов.
*   Исправление дедупликации записей лидерборда.
*   Проверка соответствия документации фактическому состоянию проекта.

### Колесова Софья: лидерборд, статистика и расчет результатов

*   Реализация экрана лидерборда и отображения результатов игроков.
*   Интеграция клиента с backend API для получения таблицы лидеров.
*   Подготовка и обработка статистики партии: счет, время выживания, убитые мобы, ресурсы и дни.
*   Реализация и проверка расчета итогового score.
*   Участие в серверной логике, связанной с сохранением и выдачей результатов.

### Елисеев Мирон: рендер, окружение и визуализация игрового мира

*   Разработка и доработка Canvas-рендера игрового мира.
*   Отрисовка карты, игрока, мобов и объектов окружения.
*   Визуализация деревьев, камней, кустов и собираемых ресурсов.
*   Отображение смены дня и ночи через затемнение сцены.
*   Поддержка визуального представления состояний мобов и окружения.

### Подмарев Александр: UI, HUD, навигация и требования

*   Доработка HUD: здоровье, голод, инвентарь, кнопки действий и виртуальный джойстик.
*   Работа с экранами приложения: главное меню, игра, авторизация, экран завершения партии и лидерборд.
*   Настройка навигации между основными экранами клиента.
*   Отображение состояния игрока и статистики текущей партии.
*   Подготовка функциональных требований и описание пользовательских сценариев MVP.

## Тестирование

В проекте используются автоматизированные тесты для проверки ключевой логики MVP.

Клиентские unit-тесты покрывают:

*   начальное состояние и сброс `GameEngine`;
*   базовые инварианты `GameState`, игрока и мобов;
*   расчет очков на клиенте;
*   выбор кадров и циклическое проигрывание анимаций.

Серверные тесты покрывают:

*   регистрацию и вход пользователя;
*   обработку некорректных учетных данных;
*   отправку результата партии;
*   валидацию payload лидерборда;
*   расчет score на сервере;
*   выдачу общего топа и личной статистики.

Запуск тестов описан в `client/docs/TESTING.md`.